use_wandb: false

dataset:
  size: 500
  d_in: &d_in 3

model:
  name: MLP
  d_in: *d_in
  d_out: 1
  hidden_layers:
    - 32
    - 32
  activation:
    name: ReLU

optim:
  name: Adam
  lr: 0.005

trainer:
  save_interval: 1.0
  epochs: 1000
  batch_size: 50
